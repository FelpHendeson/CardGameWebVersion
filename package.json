{
  "name": "card-game-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Protótipo jogável — Guerra das Cinzas e do Véu",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @duelo/web dev",
    "build": "pnpm --filter @duelo/shared build && pnpm --filter @duelo/game-data build && pnpm --filter @duelo/game-engine build && pnpm --filter @duelo/web build",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "test:e2e": "pnpm --filter @duelo/web test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^3.0.5"
  }
}
